// This file was auto-generated by Fern from our API Definition.

/**
 * @example
 *     {
 *         model: "google/nano-banana-2",
 *         prompt: "A cinematic product photo of a ceramic mug on a sunlit table",
 *         workspace_id: "ws_abc123"
 *     }
 */
export interface StartGatewayGenerationRequest {
    /** Canonical model identifier in org/model form. Use GET /gateway/models to list available models; Flora endpoint IDs from GET /models are not accepted here. */
    model: string;
    /** Generation prompt */
    prompt: string;
    /** Workspace identifier. Use the public API ID returned by list workspaces; it must start with ws_. */
    workspace_id: string;
    /** Project identifier. Use the public API ID returned by list projects; it must start with prj_. When omitted, a new project is created for this generation and returned in the response. */
    project_id?: string;
    /** Model parameters. GET /gateway/models lists each model's accepted parameters, including media inputs such as image_urls. */
    params?: Record<string, unknown>;
    /** HTTPS URL that receives a signed POST webhook when the run reaches a terminal state (events run.completed / run.failed). The JSON body is HMAC-SHA256 signed via the Flora-Signature header and delivery is retried up to 3 times with exponential backoff. Must be HTTPS and must not resolve to a private/internal host. See docs/system-overviews/webhooks.md for the payload schema and verification example. */
    callback_url?: string;
}
